<!-- HOME PAGE: save as index.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }
        body {
            background: linear-gradient(135deg, #6fb1fc, #4364f7);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
            padding: 20px;
        }
        header h1 {
            font-size: 2.5rem;
            animation: fadeIn 1s ease-in-out;
        }
        .container {
            margin-top: 40px;
            animation: slideUp 1s ease-in-out;
        }
        a.login-btn {
            display: inline-block;
            background: white;
            color: #4364f7;
            padding: 14px 32px;
            text-decoration: none;
            font-weight: bold;
            border-radius: 50px;
            font-size: 18px;
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
            transition: 0.3s;
        }
        a.login-btn:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.25);
        }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes slideUp { from { transform: translateY(40px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
    </style>
</head>
<body>
    <header>
        <h1>Welcome to My Website</h1>
    </header>
    <div class="container">
        <h2>Home Page</h2>
        <p>Explore the items below</p>

        <div style="margin-top:30px; display:flex; flex-wrap:wrap; justify-content:center; gap:20px;">
            <div style="background:white; color:#4364f7; padding:20px; width:180px; border-radius:12px; box-shadow:0 6px 15px rgba(0,0,0,0.2); font-weight:bold;">Item 1</div>
            <div style="background:white; color:#4364f7; padding:20px; width:180px; border-radius:12px; box-shadow:0 6px 15px rgba(0,0,0,0.2); font-weight:bold;">Item 2</div>
            <div style="background:white; color:#4364f7; padding:20px; width:180px; border-radius:12px; box-shadow:0 6px 15px rgba(0,0,0,0.2); font-weight:bold;">Item 3</div>
        </div>

        <a href="login.html" class="login-btn" style="margin-top:40px; display:inline-block;">Login</a>
    </div>
</body>
</html>


<!-- LOGIN PAGE: save as login.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <style>
        body {
            margin: 0;
            background: linear-gradient(135deg, #89f7fe, #66a6ff);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: "Poppins", sans-serif;
            padding: 20px;
        }
        .form-container {
            background: white;
            padding: 30px;
            width: 100%;
            max-width: 350px;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.2);
            animation: popUp 0.8s ease;
        }
        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #4364f7;
        }
        label { font-weight: 600; }
        input {
            width: 100%;
            padding: 12px;
            margin: 8px 0 18px;
            border-radius: 8px;
            border: 1px solid #b8b8b8;
            transition: 0.3s;
        }
        input:focus {
            border-color: #4364f7;
            box-shadow: 0 0 8px rgba(67,100,247,0.5);
            outline: none;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #4364f7;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
        }
        button:hover {
            background: #2c49c6;
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(0,0,0,0.2);
        }
        @keyframes popUp { from { transform: scale(0.8); opacity: 0; } to { transform: scale(1); opacity: 1; } }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>User Login</h2>
        <form>
            <label>Name:</label>
            <input type="text" placeholder="Enter your name" required>

            <label>Email:</label>
            <input type="email" placeholder="Enter your email" required>

            <label>Password:</label>
            <input type="password" placeholder="Enter password" required>

            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
